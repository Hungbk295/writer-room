{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The shell only opens a window onto the local daemon; it exposes no IPC to the page.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
